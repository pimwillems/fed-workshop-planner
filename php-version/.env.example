# Environment Configuration for Workshop Planner PHP

# Application Settings
DEBUG=false
APP_ENV=production

# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_NAME=workshop_planner
DB_USER=your_username
DB_PASSWORD=your_password

# JWT Security
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Security Settings (optional - defaults are set in config/app.php)
# CSRF_EXPIRATION=3600
# SESSION_NAME=workshop_session
# PASSWORD_MIN_LENGTH=6
# MAX_LOGIN_ATTEMPTS=5
# LOCKOUT_DURATION=900

# PostgreSQL Configuration (for data migration only)
# POSTGRES_HOST=localhost
# POSTGRES_PORT=5432
# POSTGRES_DB=workshop_planner
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=your_postgres_password